{
  "config": {
    "step": {
      "user": {
        "title": "OpenLitter",
        "description": "Connect to your OpenLitter device. The host can be a hostname (e.g. `openlitter.local`) or an IP address.",
        "data": {
          "host": "Host",
          "port": "Port",
          "ota_password": "OTA password (optional, only needed for firmware updates)",
          "use_mqtt": "Use MQTT subscription on top of REST (only if Home Assistant's MQTT integration is configured)",
          "mqtt_topic_base": "MQTT topic base"
        }
      },
      "reconfigure": {
        "title": "OpenLitter — reconfigure",
        "description": "Update the connection settings for this device. The new values take effect immediately after Submit (the entry reloads).",
        "data": {
          "host": "Host",
          "port": "Port",
          "ota_password": "OTA password",
          "use_mqtt": "Use MQTT subscription on top of REST",
          "mqtt_topic_base": "MQTT topic base"
        }
      },
      "reauth_confirm": {
        "title": "OpenLitter — reauthenticate",
        "description": "The integration has been unable to reach the device for a while. The host may have changed (e.g. a new DHCP lease). Confirm or update the connection settings.",
        "data": {
          "host": "Host",
          "port": "Port",
          "ota_password": "OTA password",
          "use_mqtt": "Use MQTT subscription on top of REST",
          "mqtt_topic_base": "MQTT topic base"
        }
      }
    },
    "error": {
      "cannot_connect": "Couldn't reach the device at that host. Check the IP / hostname and that the device is on the network."
    },
    "abort": {
      "already_configured": "This device is already configured.",
      "reauth_successful": "Reauthentication successful — connection restored.",
      "reconfigure_successful": "Configuration updated."
    }
  },
  "entity": {
    "sensor": {
      "state":        { "name": "State" },
      "weight":       { "name": "Weight" },
      "cycle_count":  { "name": "Cycle count" },
      "uptime":       { "name": "Uptime" }
    },
    "binary_sensor": {
      "cat_present":   { "name": "Cat present" },
      "home_position": { "name": "Home position" },
      "dump_position": { "name": "Dump position" },
      "error":         { "name": "Error" }
    },
    "button": {
      "cycle":  { "name": "Cycle" },
      "empty":  { "name": "Empty" },
      "reset":  { "name": "Reset" },
      "home":   { "name": "Home" },
      "pause":  { "name": "Pause" },
      "resume": { "name": "Resume" },
      "tare":   { "name": "Tare" }
    },
    "update": {
      "firmware": { "name": "Firmware" }
    }
  }
}
